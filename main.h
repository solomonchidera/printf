#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.>
#include <stdarg.h>
#include <unistd.h>
int _pu(*st);
int _putchar(char c);
int _printf(const char *format, ...);
int string_print(const char *format, ...);

#endif
